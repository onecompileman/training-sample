import { FilterBlogItemPipe } from './filter-blog-item.pipe';

describe('FilterBlogItemPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBlogItemPipe();
    expect(pipe).toBeTruthy();
  });
});
